var React = require('react');

var About = (props) => {
	return (
		<div>
			<h2>About</h2>
			<p>Welcome to the ReactJS weather finder app. Go ahead and enter a city name!</p>
		</div>
	);
}

module.exports = About;